local options = {
  ensure_installed = { "lua", "typescript", "tsx", "javascript", "markdown" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
